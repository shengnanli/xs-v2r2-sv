{
  "schema": "fm-sidecar-dead-ref-v1",
  "target": "DataPath",
  "rationale": "assembly+vmucp+dead-ref: impl side 100 percent clean (unmatched_impl=0, unread_impl=0, unread_notcompared=0). Only residual is 390 golden-only unread_ref = int_regcache_tag shadow (384) + int_regcache_enqPtr (6) top-level RegCache-hit-popcount registers, all cone-dead (fan out only to golden _GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 popcount wires with zero consumers = perf/debug counters stripped under SYNTHESIS). Impl correctly omits these redundant golden-only registers. Enumerated exactly from committed native_facts.",
  "entries": [
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[0]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[1]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[2]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[3]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[4]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_enqPtr_reg[5]",
      "rationale": "Golden-only RegCache enqueue pointer register. Indexes only into the same dead RegCache-hit popcount trees (_GEN_18[enqPtr] -> _GEN_43.._GEN_50 -> popcount) that terminate at zero-consumer wires (cone-dead). No functional output fan-out. Impl omits the redundant golden-only register."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_0_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_10_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_11_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_12_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_13_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_14_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_15_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_16_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_17_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_18_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[0]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[1]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[2]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[3]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[4]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[5]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[6]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag_19_reg[7]",
      "rationale": "Golden-only RegCache tag shadow register. Feeds only the RegCache-hit popcount trees (_GEN_57/_GEN_58/_GEN_59/_GEN_61/_GEN_64 in golden DataPath.sv) whose sink wires have ZERO consumers (terminal dead wires; perf/debug counters stripped under SYNTHESIS). No functional output fan-out (cone-dead). Impl routes RegCache reads via the precomputed rcIdx from the issue queues and omits the redundant top-level tag shadow; golden retains it."
    },
    {
      "ref_path": "r:/WORK/DataPath/int_regcache_tag